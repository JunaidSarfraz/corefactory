<% if current_user.companies.present? %>
	<div>
		<h1 class="element-inline">Work Heads</h1>
		<%= collection_select(:branch, :id, Array.new , :id, :name, {:include_blank => 'All Branches'}, {:id => "all_branches_dropdown_work_heads",:class => 'dropdown dropdown-menue btn btn-default pull-right relative-position'}) rescue nil %>
		

		<%= collection_select(:company, :id, current_user.companies, :id, :name, {:include_blank => 'All companies'}, {:id => "all_factories_dropdown_work_heads",:class => 'dropdown dropdown-menue btn btn-default pull-right relative-position'}) rescue nil %>
		<hr />
		<hr />
	</div>
<% end %>

<div id="all_work_heads">
	<%= render partial: "work_heads/work_heads_index" %>
</div>